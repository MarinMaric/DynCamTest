using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Notes : MonoBehaviour
{
    //Auto generating one point finished. May need a quadratic mode.

    //Setting a position via transposer offset or Do Nothing with basic transform

    //Make singleton

    //Give full control over control points instead of auto-generating

    //Cleanup waypoint items automatically
}